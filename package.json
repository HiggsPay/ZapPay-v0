{
  "name": "zappay",
  "version": "1.0.0",
  "description": "ZapPay — crypto payment gateway built on the x402 protocol",
  "private": true,
  "scripts": {
    "dev": "cd server && npm run dev",
    "install:all": "npm install && cd server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 